# Warframe Reward Price Checker (для Linux)

Скрипт на Python для игроков Warframe на **Linux**, который автоматически проверяет цены на награды с экрана выбора после миссии.

Этот инструмент создан как альтернатива популярным Windows-программам (таким как Warframe Info, AlecaFrame), которых часто не хватает в Linux-сообществе. Особое внимание уделено стабильной работе в окружении **Wayland**, так как скрипт не использует сторонние средства захвата экрана, а просто отслеживает появление новых файлов в папке.

## Как это работает?

Скрипт отслеживает указанную папку. Как только в ней появляется новый скриншот, он:
1.  **Обрабатывает изображение** с помощью OpenCV, чтобы выделить область с текстом.
2.  **Распознаёт названия предметов** на русском языке с помощью EasyOCR.
3.  **Отправляет запрос** к API Warframe.Market, находит наиболее подходящее название и получает актуальные цены.
4.  **Выводит результат** в консоль.

---

## Важно: Как правильно делать скриншот

Для корректной работы скрипта крайне важно, чтобы в область вашего скриншота попали **только названия четырех наград**. Не нужно захватывать весь экран.

**Правильный пример (`items.png`):**
![Пример необходимого скриншота](items.png)

Захватите только ту область, где расположены названия предметов, как показано на изображении выше. Любые лишние элементы интерфейса могут помешать распознаванию.

---

## Ключевые особенности

-   ✅ **Создан для Linux**: Решает проблему отсутствия удобных оверлеев и чекеров цен.
-   ✨ **Идеально для Wayland**: Не использует захват экрана, который часто вызывает проблемы в Wayland. Скрипт просто следит за созданием файлов, что работает надёжно везде.
-   🇷🇺 **Для русскоязычного сообщества**: Распознавание текста и запросы к API настроены для русского языка.
-   📈 **Быстрая оценка цены**: Помогает быстро принять решение, стоит ли брать редкую деталь или лучше выбрать уже собранный предмет.

---

## Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/xineigor/warframe-price-checker-linux.git
    cd ВАШ_РЕПОЗИТОРИЙ
    ```

2.  **Убедитесь, что у вас установлен Python 3.8+.**

3.  **Создайте и активируйте виртуальное окружение (рекомендуется):**
    ```bash
    python -m venv venv
    source venv/bin/activate
    ```

4.  **Установите все зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    *Примечание: При первом запуске EasyOCR может автоматически скачать модели для распознавания, что займёт некоторое время.*

## Использование

1.  **Запустите скрипт в терминале:**
    ```bash
    python main.py
    ```
    Скрипт начнёт отслеживать ту папку, из которой он был запущен.

2.  **В игре, на экране выбора наград, сделайте скриншот.**
    Используйте любой удобный вам инструмент (например, `grim` в Sway/Hyprland или встроенный инструмент в GNOME/KDE) и выделите **только область с названиями**, как показано в примере `items.png`.

3.  **Сохраните скриншот** в папку, где запущен скрипт.

4.  **Смотрите результат в терминале.**
    Скрипт мгновенно подхватит новый файл и выведет цены.

## Как читать цены?

Скрипт запрашивает **5 самых низких цен** у продавцов, которые имеют статус "в сети" (`online`) или "в игре" (`in-game`) на Warframe.Market.

Это не полная статистика, а скорее срез актуальных предложений от активных игроков. Как по мне, это лучший способ примерно определить цену, за которую можно **сразу** продать предмет, не ожидая долго.

Пример вывода:
```
Никана Прайм Клинок -> [Никана Прайм: Клинок] 80, 80, 81, 82, 83 платины
Форма - Не продается
Аксомати Прайм Приклад 10, 11, 11, 12, 12 платины
```
Здесь `-> [Никана Прайм: Клинок]` означает, что распознанное "Никана Прайм Клинок" было успешно сопоставлено с официальным названием в базе Warframe.Market.
